<?php
    const file_name = "etudiant.txt";
    const parameter_out = "%s; %s; %s; %d; %s; %s; %f; %f; %f; %f; %f\n";
    const parameter_int = "%[^;]; %[^;]; %[^;]; %d; %[^;]; %[^;]; %f; %f; %f; %f; %f\n";
